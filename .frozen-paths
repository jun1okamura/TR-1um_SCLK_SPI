# `apr/check_no_home_paths.py` が見ない場所（U93）。
# **1 行 1 つ、理由を必ず書く。** 理由の無い行は読まれない。
# ここに書いてよいのは「**中身が当時の記録**で、書き換えると意味を失うもの」だけ。
# 生成物なら追跡をやめる。機能する道なら直す。**逃げ場にしない。**
scripts/i2c_ref/   # TR-1um_Async_I2C からの移植の島。当時の作業場（/sessions/…）の絶対パスを含む。場所は動かせない（README / PORTING.md / live な scripts が名前で参照している）
scripts/port_i2c_scripts.py   # 上の島を移植したときの記録。どのファイルをどこから持ってきたかを絶対パスで残してある
scripts/PORTING.md   # 同上（移植の経緯そのもの）
