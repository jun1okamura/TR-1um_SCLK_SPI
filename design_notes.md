# TR-1um_SCLK_SPI 設計ノート

最終更新: 2026-09-07

---

## 1. 仕様

SCLKドメイン同期式の**3線式(半二重)SPIスレーブ**コア。既存の
[`TR-1um_I2C_2026`](../TR-1um_I2C_2026) / [`TR-1um_Async_I2C`](../TR-1um_Async_I2C)
と同一の外部データインタフェース(`DATA[7:0]`双方向共有パッド + `DIS` + `RSTN`)
を持ち、シリアルプロトコル層だけをI2C→SPIに置き換えた構成。

- **モード**: Mode 0 (CPOL=0 / CPHA=0)、MSBファースト
- **フレーム**: 8bit単発(CS↓ → SCLK×8 → CS↑)。コマンド/アドレスバイトなし、バーストなし
- **半二重**: MOSI/MISOは `SDIO` 1本に統合
- **方向決定**: `DIS`。同じピンが `DATA[7:0]` の方向も決めるため、R/Wビットもコマンドバイトも不要

| DIS | フレーム | SDIO | DATA[7:0] | 動作 |
|:---:|---|---|---|---|
| 0 | WRITE | 入力(マスタが駆動) | **チップが駆動** | 受信バイトを `rx_data` に確定しDATAへ出力 |
| 1 | READ | **チップが駆動**(CS=Low中) | 入力(`tx_data`) | DATAを取り込みSDIOへMSBから送出 |

---

## 2. ピン割り当て

`OSS_FRAME` は16パッド。VDD/VSSを除く信号パッドは14本(P8は存在しない)。
`RING_OSC` は搭載しない。

| パッド | 信号 | 方向 | 説明 |
|---|---|---|---|
| P1 | `SCLK` | 入力 | SPIクロック。本コア唯一のクロック源(`BUFTH`挿入) |
| P2 | `SDIO` | 双方向 | SPIデータ(半二重)。入力パスに`BUFTH` |
| P3 | `CS` | 入力 | チップセレクト(負論理)。フレーム境界を規定(`BUFTH`挿入) |
| P4 | `TEST` | 出力 | `byte_end` — フレーム第8ビット期間中High |
| P5 | `DIS` | 入力 | フレーム方向 + `DATA[7:0]`出力ディセーブル |
| P6 | `DATA[0]` | 双方向 | 汎用データ bit0 |
| P7 | `DATA[1]` | 双方向 | bit1 |
| P9〜P14 | `DATA[2..7]` | 双方向 | bit2〜bit7 |
| P15 | `RSTN` | 入力 | 非同期リセット(負論理) |

物理パッド番号の昇順とbit番号が単調対応(P6/P7=bit0-1、P9〜P14=bit2-7)。
チップレベルTB(`tb_12_chip`)でこの対応を1本ずつ検証済み。

---

## 3. アーキテクチャ

半二重化により**シフトレジスタが1本で済む**のが構成上の要点。

```
          +--- bit_cnt[2:0] (posedge SCLK, CS で非同期クリア)
          |
SCLK --+--+----------------------------------> rx_data ホールド (posedge SCLK)
       |                                             ^
       +--[XOR]--> shift_clk --> 共用シフトレジスタ sr[7:0]
       DIS ^                        |    ^
           |                        |    +--- tx_data[6:0] パラレルロード
           |                        +-------> SDIO 出力 (sr[7])
           +----------------------> msb_done (posedge shift_clk) --> MSB選択
```

- `shift_clk = SCLK ^ DIS`
  - WRITE(DIS=0): `posedge SCLK` — SDIOを取り込む
  - READ (DIS=1): `negedge SCLK` — Mode 0が要求する「立下りで出力更新」を満たす
  - `DIS` はフレーム中静止しているので、この選択がCS=Low中に切り替わることはない
- Mode 0 は最初の立上り前にMSBが確定している必要があり、その時点ではまだクロック
  エッジが存在しない。よって bit7 のみ `tx_data[7]` から出力し、bit6以降はシフト
  レジスタから出す(`sr` は最初の立下り、`bit_cnt==1` でパラレルロード)。
- **`msb_done`**: 上記のMSB選択を `(bit_cnt == 0)` で直接行うと、`bit_cnt` が変化する
  第1・第8**立上り**エッジでSDIOが動いてしまう(§4で検出した不具合)。選択信号を
  `shift_clk` で叩いたFFにすることで、SDIOの遷移を立下りエッジだけに閉じ込め、
  かつバイト境界ごとに再アームして複数バイト転送も維持している。追加コストは
  `DFFRB`×1 + `OR3`×1 − `NOR3`×1 ≈ **+28 Tr(+7等価ゲート)**。
- `bit_cnt` は `CS`=High で非同期クリアされるため、アイドル中にSCLKがフリーラン
  しても次フレームは必ずbit7から始まる。`sr` にゴミが入っても8エッジ後には正しい
  8ビットに置き換わるので無害 — この性質を使って `sr` のCSゲートを省いている。

---

## 4. 検証

I2C版と同じ2段階等価性検証を、変換後ネットリストまで拡張した**3ビュー**構成。
全テストベンチが同一ソースのまま3つのビューに対して実行される。

| ビュー | 対象 |
|---|---|
| RTL | `hdl/spi_slave_sclk.v` |
| NET | `layout/spi_slave_sclk_net.v`(Yosys/ABC合成直後) |
| PNR | `layout/spi_slave_sclk_net_pnr.v`(MUXDFFRB統合 + BUFTH + 行バッファ挿入後) |

```sh
scripts/run_tests.sh                      # 全12本 × 3ビュー
scripts/run_tests.sh tb_05_mode0          # 1本だけ
DUMP=1 scripts/run_tests.sh tb_05_mode0   # 波形(VCD)付き
```

### テストベンチ一覧

| # | ファイル | チェック数 | 内容 |
|---|---|---:|---|
| 01 | `tb_01_reset.v` | 11 | 電源投入時リセット、アイドル時パッド状態、アイドル中/フレーム途中/CS=Low中の非同期リセットと復帰 |
| 02 | `tb_02_write.v` | 20 | WRITEフレーム10パターン(コーナー/ウォーキング)を `rx_data` とDATAパッド両方で確認 |
| 03 | `tb_03_read.v` | 11 | READフレーム10パターン + READが `rx_data` を壊さないこと |
| 04 | `tb_04_bitorder.v` | 32 | MSBファースト検証。両方向のウォーキング1/ウォーキング0 |
| 05 | `tb_05_mode0.v` | 13 | Mode 0エッジ挙動。最初の立上り前のMSB確定、**全立上りエッジでのSDIO安定**、`tx_data` が第1立下りでラッチされ以降無視されること、MOSIが立上りだけでサンプルされること |
| 06 | `tb_06_tristate.v` | 13 | `DIS`×`CS` の全組合せでのSDIO/DATA駆動・Hi-Z、フレーム中の競合が無いこと |
| 07 | `tb_07_idle.v` | 9 | 20µsアイドル保持、SCLKフリーラン耐性、クロック無しCSパルス、5クロックの中断フレーム、9クロック過剰時の挙動と再同期 |
| 08 | `tb_08_multibyte.v` | 10 | CSを下げたままの3/8バイトWRITE、4バイトREAD、バイト間で `tx_data` を変えた場合の再サンプル |
| 09 | `tb_09_clockrate.v` | 16 | SCLK 100kHz〜20MHzのスイープ + デューティ5%/95%の非対称波形 |
| 10 | `tb_10_random.v` | 1 | 200トランザクションのランダム回帰(方向・データ・メッセージ長・アイドル間隔)をゴールデンモデルと照合 |
| 11 | `tb_11_spidev.v` | 20 | ラズパイ `spi-bcm2835` 3線モードのシーケンス再現。`writebytes`/`readbytes` 相当、DIS操作順序、W/R交互、**`xfer2()` 使用時に競合が起きることの負検証**と復帰 |
| 12 | `tb_12_chip.v` | 31 | チップレベル。実パッドP1〜P15を駆動し、DATA 8本のビット対応をワンホット走査で全数検証。TESTパッド、`BUFTH` 経路、P15リセット |
| | **合計** | **187** | **× 3ビュー = 561チェック / 36ラン、全PASS** |

共通のSPI Mode0マスタBFMとスコアボードは `hdl/spi_tb_common.vh`。双方向ネットを
そのままモデル化しているので、パッド競合はXとして検出される。

### 検出した不具合(修正済み)

`tb_05_mode0` が **SDIOが第1・第8の立上りエッジで変化する**Mode 0違反を検出。
`sdio_out` のMSB選択を `(bit_cnt == 0)` で直接行っていたため、`bit_cnt` が更新
される立上りエッジでSDIOが動いていた。マスタは立上りでサンプルするので読み値
自体は正しく、他の11本のテストベンチは全て通っていたが、マスタのホールド時間
マージンを `bit_cnt` のclk-to-Q分しか残さない設計だった。§3の `msb_done` FFを
追加して全遷移を立下りエッジに閉じ込め、解消。

---

## 5. ゲート規模(合成実測)

セル面積は `lef/TR-1um_STDCELL.lef` の **MACRO SIZE**(= 実際のフットプリント)、
トランジスタ数は `TR-1um_Async_I2C/LEF/<cell>.extracted` のMOS素子数
(`lef/cell_info.json`)。合成用Libertyはこの実面積で生成しているので
(`scripts/gen_liberty.py`)、ABCが本物のシリコン面積で最適化する。

> **1等価ゲート = `NAND2` = 4 Tr = 16.2 × 64.8 = 1,049.8 µm²**

⚠ セルのGDSバウンディングボックスはフットプリントではない。全セルが
prBoundaryからx方向に12.6 µm、y方向に4.0 µm はみ出す(隣接セルと重なる
ウェル/インプラント囲み)ため、GDS実測だと `INV_X1` が 1,610 µm² に見えるが
LEFの正は 700 µm²。面積系の数字はすべてLEF基準に統一してある。

### 工程ごとの推移

| 工程 | セル | Tr | 等価ゲート | 面積 (µm²) |
|---|---:|---:|---:|---:|
| 合成直後 (`_net.v`) | 49 | 794 | 198 | 135,418 |
| MUXDFFRB統合 (`_net_merged.v`) | 34 | 794 | 198 | 135,420 |
| **BUFTH+行バッファ (`_net_pnr.v`)** | **41** | **842** | **210** | **145,917** |

MUXDFFRB統合は `MUX2`+`DFFRB` の15ペアを統合(トランジスタ数不変、面積 −13,005 µm²)。
残る `MUX2`×1 は `sdio_out` のMSB選択で、DFFのDピンを駆動していないため対象外。

### 最終構成(配置配線投入時)

| セル | 個数 | Tr | 面積 (µm²) |
|---|---:|---:|---:|
| `MUXDFFRB` | 15 | 570 | 94,479 |
| `DFFRB` | 5 | 130 | 20,995 |
| `BUF_X2`(行バッファ) | 4 | 24 | 4,199 |
| `BUFTH`(SCLK / SDIO / CS) | 3 | 24 | 6,298 |
| `XOR2` | 2 | 20 | 3,499 |
| `NOR4` | 2 | 16 | 3,499 |
| `AND2_X1` | 2 | 12 | 2,799 |
| `NOR2` | 2 | 8 | 2,100 |
| `INV_X1` | 2 | 4 | 1,400 |
| `MUX2` | 1 | 12 | 2,100 |
| `OR3` | 1 | 8 | 1,750 |
| `XNOR2` | 1 | 10 | 1,750 |
| `NAND2` | 1 | 4 | 1,050 |
| **合計** | **41** | **842** | **145,917** |

記憶素子 20個。行バッファは2行構成(§12)で、実配置で得た行割り当てを
`scripts/insert_row_buffers.py --row-assignment` に戻した2パス目の結果(§13.2)。

> ### 結論
> **等価ゲート 210 / トランジスタ 842 / 論理セル 41個 / 記憶素子 20個 /
> 論理セル面積 0.146 mm²**
>
> 配置配線まで完了した**実際のコア面積は 1,632.6 × 324.9 µm = 0.5304 mm²**
> (§14.4)。

---

## 6. I2C版との比較

| 項目 | SPI版 | I2C版(実測) | 比 |
|---|---:|---:|---:|
| 論理セル数 | 41 | 126 | 0.33 |
| 記憶素子数 | 20 | 36 | 0.56 |
| トランジスタ数 | 842 | 1,598 | 0.53 |
| **等価ゲート数** | **210** | 400 | 0.53 |
| 論理セル面積 | 0.146 mm² | 0.299 mm² | 0.49 |
| **配置配線後コア面積** | **0.5304 mm²** | 1.533 mm² | **0.35** |

いずれもLEFのMACRO SIZE基準。コア面積は両者とも実際の配置配線結果。

**約半分**に収まる。差分の内訳:

- SPIは `CS` がフレーム境界を明示するため、I2Cのプロトコル層 — START/STOP検出、
  ACK/NACK生成・応答、7bitアドレス比較器、スパイクフィルタ、SDAオープンドレイン
  制御 — が**まるごと不要**。I2C版で `NAND2` 23個・`NOR2` 14個を要したのはこの
  非同期プロトコル層。
- 半二重化により**シフトレジスタが1本で済む**(全二重ならRX/TXで2本必要)。
- I2C版が非同期(クロックレス)設計だったのに対し、SPI版は `SCLK` 単一ドメインの
  純同期設計。レース対策のための冗長な論理が不要。

---

## 7. 見積もり履歴

| 版 | 構成 | 等価ゲート | Tr | 根拠 |
|---|---|---:|---:|---|
| 初版 | I2C同等I/F、コマンド+データ2バイト | 約 400 | — | 汎用的な机上見積もり |
| 2版 | 全二重、8bit単発、`DATA`+`DIS` | 約 270 | 1,070 | I2C実チップのセル実測でキャリブレーション |
| 3版 | 半二重3線 | 約 210 | 834 | RTL机上構成 |
| 4版 | 半二重3線 + Mode0修正 + BUFTH/行バッファ(4行) | 212 | 850 | 合成〜P&R投入ネットリストの実測値 |
| **確定** | **同左、2行構成 + `BUF_X2`** | **210** | **842** | **配置配線まで完了した最終ネットリスト** |

---

## 8. 設計上の注意点

- **`DIS` はCS=Low中に変化させてはならない。** `shift_clk = SCLK ^ DIS` のため、
  フレーム途中で `DIS` が変わるとシフトレジスタのクロックにグリッチが乗る。
  CS=High中の変化は無害。
- **READ時の `tx_data`(DATAピン入力)はフレーム中に安定していること。**
  bit7 のみ組合せ経路で出力するため、CS↓から最初の立下りまでの間に `tx_data[7]`
  が変化すると誤ったMSBが出る(`tb_05_mode0` で検証)。
- **クロックネットは2本**(`SCLK` と `shift_clk`)。AP&R時の行分配は両系統に必要。
  現状の暫定ネットリストでは各4行 = `BUF_X1`×8。
- `BUFTH` は `SCLK` / `SDIO`(入力パス) / `CS` に挿入(`scripts/insert_bufth.py`)。
- `CS` は `SCLK` に対して非同期。`bit_cnt` の非同期クリアに使い、ロードおよび
  `rx_data` 更新自体はSCLKエッジで行う。最終SCLKエッジからCS↑までのホールド時間は
  実機仕様として規定が必要。
- `TEST`(P4)の `byte_end` は `bit_cnt` のデコード出力なので、カウンタ遷移時に
  短いグリッチが乗りうる。オシロでのフレーム確認用途に限る。
- Liberty はプレースホルダ。**面積・ゲート規模の見積もり専用**であり、タイミング
  値はダミーなのでSTAには使えない。

---

## 9. ディレクトリ構成

| ディレクトリ | 内容 |
|---|---|
| `hdl/` | コアRTL、チップトップ、セルモデル、共通BFM(`spi_tb_common.vh`)、テストベンチ12本 |
| `lef/` | `TR-1um_STDCELL.gds` / `.lef`(TR-1um_Async_I2Cからコピー)、`TR1um_5_stdcell.lib`(I2C版参照用)、`TR1um_5_stdcell_area.lib`(実面積版、生成物)、`cell_info.json` |
| `layout/` | 合成後NET、各変換段のNET、`synth.ys`(生成物)、以降の配置配線中間成果 |
| `irsim/` | IRSIM検証(未着手) |
| `scripts/` | 合成・ネットリスト変換・規模レポート・検証実行。詳細は [`scripts/SCRIPTS.md`](scripts/SCRIPTS.md) |
| `src/` | **TO(テープアウト)提出用** `tr_1um_3wire_SPI.gds` / `.cir` |
| `docs/` | `info.md` データシート、チップ画像 |

```sh
scripts/build.sh        # 合成 → MUXDFFRB統合 → BUFTH → 行バッファ → 規模レポート
scripts/run_tests.sh    # 全テストベンチ × 全ビュー
```

---

## 10. Raspberry Pi からの接続(Pi 4以前 / チップ3V動作)

### 10.1 ラズパイ側の3線SPI対応状況

`raspberrypi/linux` の `rpi-6.12.y` のドライバソースで確認(2026-09時点)。

| モデル | SPIコントローラ / ドライバ | `SPI_3WIRE` |
|---|---|---|
| **Pi 4 / 3 / Zero など(BCM2835〜BCM2711)** | SPI0 / `spi-bcm2835` | **対応** |
| Pi 5 (RP1) | `snps,dw-apb-ssi` / `spi-dw` | 非対応 |

- `spi-bcm2835.c`: `BCM2835_SPI_MODE_BITS = (SPI_CPOL｜SPI_CPHA｜SPI_CS_HIGH｜SPI_NO_CS｜SPI_3WIRE)`
- 双方向モードでは **MOSIピン(GPIO10 / ヘッダ19番)が MOMI**(単線データ)として
  働き、MISO(GPIO9)は未使用。受信時にドライバがCSレジスタの **REN** ビットを
  立てて送信ドライバをHi-Zにする。
- **制約**: `spi_transfer` の `tx_buf` と `rx_buf` は一方が必ず NULL でなければ
  ならない。`spidev_test.c` がこれを守らず3線モードで失敗するのは既知。
  → **本チップは `DIS` がフレーム方向を決めるため、この制約に完全に合致する。**

| | DIS | spidev | REN | 線の駆動 |
|---|:---:|---|:---:|---|
| WRITE | 0 | `writebytes()` | 0 | ラズパイのMOSIが駆動 |
| READ | 1 | `readbytes()` | 1 | MOSIはHi-Z、チップが駆動 |

DMA有効時に3線が壊れる不具合が2019年にあったが(`spi_map_msg()` がNULLの
`rx_buf` をダミーバッファに置換するため)、`master->dummy_rx` を除外する修正が
4.14以降のstableにバックポート済み。

### 10.2 結線 — レベル変換は不要

チップを**3V系で動かせるため、レベル変換器は一切不要**。

> **最も安全な構成: チップの VDD をラズパイの 3.3V レール(ヘッダ1番/17番)から
> 給電する。** 両者が同一電位になるので、入出力とも直結でよい。

チップを3.0Vなど3.3Vより低い電圧で動かす場合の注意:

- **ラズパイ → チップ**: 3.3V出力がVDD(3.0V)を0.3V超える。ESD保護ダイオードの
  順方向電圧(約0.6V)には届かないので通常は問題ないが、マージンを取るなら
  ラズパイ側出力に直列1kΩを入れる。
- **チップ → ラズパイ**: 3.0V出力はBCM2835のVIH(約2.3V)を十分上回るので問題なし。

| ラズパイ | ヘッダ | チップ | 備考 |
|---|---|---|---|
| GPIO11 / SCLK | 23 | P1 `SCLK` | |
| GPIO10 / MOSI(**MOMI**) | 19 | P2 `SDIO` | 3線モードではここが単線データ |
| GPIO9 / MISO | 21 | — | 3線モードでは**未使用** |
| GPIO8 / CE0 | 24 | P3 `CS` | spidevがフレーム毎にアサート |
| 任意GPIO(入力) | — | P4 `TEST` | デバッグ用、省略可 |
| 任意GPIO(出力) | — | P5 `DIS` | ioctlの前に設定 |
| 任意GPIO ×8 | — | P6,P7,P9〜P14 `DATA[7:0]` | WRITE時は入力設定、READ時は出力設定 |
| 任意GPIO(出力) | — | P15 `RSTN` | |
| 3.3V | 1 / 17 | VDD | |
| GND | 6 / 9 / … | VSS | 複数点で接続 |

- `SDIO` に弱プルダウン(100kΩ程度)を入れ、`DIS`=1 かつ `CS`=High で誰も駆動
  しない期間のフローティングを避ける
- 必要GPIOは14本。40ピンヘッダで十分足りる

### 10.3 spidev からの操作

```python
import spidev
from gpiozero import DigitalOutputDevice

spi = spidev.SpiDev(); spi.open(0, 0)
spi.mode          = 0b00       # Mode 0
spi.bits_per_word = 8
spi.threewire     = True       # SPI_3WIRE (Pi 4以前のみ)
spi.max_speed_hz  = 100_000    # まず低速から

dis = DigitalOutputDevice(<GPIO>)

# WRITE : DIS=0 にしてから 1バイト送る -> DATA[7:0] に出る
dis.off(); spi.writebytes([0xA5])

# READ  : DIS=1 にしてから 1バイト読む <- DATA[7:0] の値が返る
dis.on();  val = spi.readbytes(1)[0]
```

- `writebytes` / `readbytes` はそれぞれ片方向の `spi_transfer` になるため、
  3線モードの tx/rx NULL 制約を自然に満たす。**`xfer` / `xfer2`(全二重)は
  使わないこと** — 使うとREADフレーム中にマスタがSDIOを駆動してチップと競合する
  (`tb_11_spidev` の負検証で確認済み)。
- `DIS` の変更は `CS` がHighの間に行う(§8)。ioctlの前後でGPIOを操作すれば
  この順序は自然に守られる。
- CE0 は1バイト転送につき8クロック分だけLowになり、フレーム定義と一致する。

### 10.4 複数バイト転送(CSを下げたまま)

spidev に2バイト以上渡すと `CS` を下げたまま 8N クロックが出る。本設計は
`bit_cnt` がラップして次フレームに入るため正しく動作する(`tb_08_multibyte`)。

| 操作 | 結果 |
|---|---|
| CS Lowのまま3バイトWRITE `11 22 33` | `rx_data` = 最後のバイト `0x33` |
| CS Lowのまま4バイトREAD | 全バイトとも `tx_data` の値(フレームごとに再サンプル) |
| バイト間で `tx_data` を変更 | 次バイトから新しい値が出る |

### 10.5 クロック速度

1µmプロセス・3V動作なので **100kHz程度から始めて段階的に上げる**。参考として
I2C版チップのリング発振器実測は5V時に97段で6.5MHz(段遅延 ≒ 0.79ns)。3V動作では
これより遅くなる。BCM2835 のSPIクロックは分周比が2の冪なので、`max_speed_hz` の
設定値そのままにはならない。RTLは100kHz〜20MHzで検証済み(`tb_09_clockrate`)。

---

## 11. STDCELLへの追加提案

> 優先度1の `BUF_X2` は 2026-09-07 に実装済み。実測結果は §12.1 参照。

現行の `TR1um_5_stdcell` は論理セル25種(`INV_X1` `BUF_X1` `BUFTH` `AND2/3/4_X1`
`OR2/3/4` `NAND2/3/4` `NOR2/3/4` `XOR2` `XNOR2` `MUX2` `DEL1/2/4` `DFFRB` `DFFS`
`RSLATCH` `MUXDFFRB`)。本設計の合成結果から、以下を優先度順に提案する。

### 優先度1: `BUF_X2` / `BUF_X4` / `INV_X2` / `INV_X4` — ドライブ強度の系列

**現行ライブラリにはX1しか無い。** 本設計はクロックネットが2本(`SCLK` と
`shift_clk`)あり、各行の `MUXDFFRB` のCKピン群と長いM2配線を `BUF_X1`(4 Tr)
1個で駆動することになる。1µm・3V動作ではこれが**最大SCLK周波数と行間スキューを
直接律速する**。X2/X4があれば1行1段で済み、バッファ段数と行間スキューの両方が
減る。面積コストも小さい(X2で+2〜4 Tr程度)。

**本チップで最も効く追加。** AP&R前に用意できるのが理想。

### 優先度2: `DFFRB_Q` / `MUXDFFRB_Q` — QB出力を持たないFF

合成結果を実測したところ、**20個のFFのうち17個で `QB` がどこにも接続されていない**。
QB用の出力インバータを削ったバリアントを用意すれば、1個あたり約2 Tr・幅5µm程度
(≒371 µm²)が浮く。17個で **約6,300 µm²、コア面積の約3%、等価ゲート換算で約8ゲート**
の削減。既存セルからインバータを1段落とすだけなので、設計コストも低い。

### 優先度3: `NAND2B` / `NOR2B` — 片側入力反転の2入力ゲート

`NAND2`/`NOR2` と同じ4 Trで、片方の入力だけがバブル付き。**実測**: これらを
Libertyに追加して再合成したところ、ABCが `NOR2B` を4個使い、
セル 34→33個、Tr 794→788、面積 173,244→170,890 µm²(−1.4%)になった。
本設計では小幅だが、追加コストがゼロ(同じトランジスタ数)なので入れて損はない。

### 優先度4: `AOI21` / `OAI21` / `AOI22` / `OAI22` — 複合ゲート

**現行ライブラリには複合ゲートが1つも無い。** これはCMOS標準セルライブラリとしては
大きな欠落で、ABCの技術マッピングが本来最も得意とする形が使えていない。
`AOI21`(6 Tr)は `AND2_X1`+`NOR2`(10 Tr)を置き換える。

ただし**本設計での実測利得はゼロだった** — 組合せ論理が11セルしかなく、ABCは
`NOR2B` の方を選んだ。効いてくるのは組合せ論理を持つ将来の設計(ALU、比較器、
カウンタチェーン等)。**今回のテープアウトのためというより、ライブラリ整備として**
の提案。

### 優先度5: `XOR2_CLK`(または `CLKMUX2`) — クロック経路用

本設計は `sclk ^ dis` を**クロック経路に置いている**。汎用 `XOR2` はA→YとB→Yの
遅延が非対称で、立上り/立下り遅延も揃っていないため、`shift_clk` のデューティ
歪みになって現れ、最大SCLKを制限する。入力間で遅延を揃え、ドライブを上げた
クロック用XOR(または等価なクロックMUX)があると素直。

### 優先度6: `TIEHI` / `TIELO`

未使用入力を電源レールに直結せずに固定するための標準セル。I2C版では
`add_hiz_vss_ties_v9.py` でレイアウト側に処理を寄せているが、セルがあれば
ネットリスト段階で扱えてアンテナ/ESD上も素直になる。

---

## 12. 配置 — 行数の検討

### 12.1 BUF_X2(2026-09-07 実装)

`lef/TR-1um_STDCELL.gds` に追加された `BUF_X2` をGDSから実測:

| | フットプリント | 段構成 | PMOS 合計W | NMOS 合計W | Tr |
|---|---|---|---:|---:|---:|
| `BUF_X1` | 16.2 × 64.8 µm | 10.2/3.4 → 10.2/3.4 | 20.4 µm | 6.8 µm | 4 |
| `BUF_X2` | 16.2 × 64.8 µm | 10.2/3.4 → **10.2/3.4 ×2並列** | 30.6 µm | 10.2 µm | 6 |

出力段が2フィンガ並列になり **W が2倍**。**フットプリントは `BUF_X1` と同一**
なので、行バッファを `BUF_X2` に置き換えても面積は1セルも増えない。
`scripts/build.sh` の既定を `ROW_BUF_CELL=BUF_X2` にした。
`BUF_X4` / `BUF_X16` はLEFには登録済みだがGDSには未実装。

### 12.2 行数の比較

行幅を **I2C版と同じ 1620 µm に固定**して検討した(GIO⇔コアの結線枠組みを
I2C版からそのまま引き継げるため)。`scripts/explore_rows.py --row-width 1620`。

セル総幅は 2,251.8 µm(2行構成のネットリスト)なので、**1行(1,620 µm)には
入らず、2行が最小**。行幅を固定すると余りはFILLになるだけなので、行数を
増やすほど面積は単調に悪化する:

| 行数 | 行容量 | セル総幅 | コア W×H | 面積 | 備考 |
|---:|---:|---:|---|---:|---|
| 1 | 1,620 µm | 2,251.8 µm | — | — | **入らない** |
| **2** | **3,240 µm** | **2,251.8 µm** | **1,620 × 254 µm** | **0.411 mm²** | **採用** |
| 3 | 4,860 µm | 2,284.2 µm | 1,620 × 351 µm | 0.569 mm² | 分割すると3行目が空になる |

3行を指定しても、分割器は行容量に余裕があるため実質2行に詰めてしまう
(3行目の占有率0%)。**2行で決まり。**

#### 2行構成の詳細

行内の配分は2通り比較した(コア高さはどちらも 254 µm で同じ):

| | 行0 / 行1 の幅 | 占有率 | チャネル(トラック) | 最大チャネル |
|---|---|---|---|---:|
| バランス配置(推奨) | 961.2 / 1,290.6 µm | 59% / 80% | 7 / 6 / 10 | **10 trk** |
| 詰め込み配置 | 1,555.2 / 696.6 µm | 96% / 43% | 13 / 6 / 4 | 13 trk |

**バランス配置を推奨。** 面積は同じで最大チャネルが 13→10 トラックに減り、
かつ各行に 300〜650 µm の空きが残るのでルータのジョグ・フィードスルーに
余裕ができる(占有率96%の行は逃げ場が無い)。

- コア: **1,620 × 254 µm = 0.411 mm²**(行 2×64.8 = 129.6 µm + チャネル
  38 / 32 / 54 µm)
- I2C版コア 1,632.6 × 938.9 = 1.533 mm² の **27%**。**幅が一致する**ので
  GIO⇔コアの結線・電源メッシュの枠組みをそのまま流用できる
- FILL 必要量は計 988 µm 分(≒ 64,000 µm²)
- アスペクト比 6.4:1 と扁平。2.5×2.5 mm フレーム内では上下に大きく空きが
  出るので、ロゴ等の配置余地は十分

#### ⚠ クロック分配の確認事項

2行構成では、1つの行バッファが**最大 1.29 mm の行全体**のFFクロックピンを
駆動する。ざっくり見積もると M2 配線容量が 6個のCKゲート容量と同等かそれ
以上になり、**クロック枝は配線容量支配**になる。`BUF_X2`(出力段
W=20.4/6.8 µm)で足りるかはレイアウト抽出後のSPICEで確認すべき項目。
足りなければLEFに登録済みの `BUF_X4` を実装するのが素直
(フットプリントが `BUF_X1` と同じなら面積増もゼロ)。

### 12.3 現状のP&R投入ネットリスト

`layout/spi_slave_sclk_net_pnr.v` — **41セル / 842 Tr / 210等価ゲート /
論理セル面積 0.146 mm²**、`BUFTH`×3 + `BUF_X2`×4(2行 × クロック2系統)。
全36テストランPASS済み。`scripts/build.sh` の既定は `ROWS=2`。

行バッファの配分は現状「シンクを2等分」の暫定値。実配置で行割り当てJSONが
得られたら次で入れ直す:

```sh
scripts/insert_row_buffers.py layout/spi_slave_sclk_net_bufth.v \
    layout/spi_slave_sclk_net_pnr.v --nets sclk_buf,shift_clk \
    --cell BUF_X2 --row-assignment layout/row_assignment.json
scripts/run_tests.sh
```

---

## 13. 配置(Placement)

`scripts/place.py`。配置規約はI2C実チップのGDS
(`TR-1um_I2C_2026/src/tr_1um_i2c_slave_async.gds`)を解析して実測した:

- セルは **prBoundary(レイヤ235/0、全セルで 0..W × 0..64.8)でアバット**。
  GDSのbboxはprBoundaryからx方向に12.6µm、y方向に4.0µmはみ出す(隣接セルと
  重なるウェル/インプラント囲み)ので、配置座標はprBoundary基準
- 全参照が **回転0・反転なし**。行のミラーリングはしない
- 各行は **x=0 から行幅ちょうどまで隙間なく充填**
- **TAP2 は x = 0 / 534.6 / 1069.2 / 行幅−10.8** の固定位置(ピッチ534.6µm)
- 残りは FILL3(16.2)/ FILL2(10.8)で埋める。**FILLは行内に分散配置**
  (ルータのフィードスルー・ジョグ用の隙間を確保するため)

FILL2/FILL3 では 5.4µm ちょうどの隙間だけが作れないため、配置器はセルを
セグメントに詰める際「残りが 5.4µm になる置き方」を避けて次セグメントへ送る。

### 13.1 STEP構成(各STEPのGDSを残す)

| STEP | 内容 | 成果物 |
|---|---|---|
| step1 | 行割り当て(FM分割)。セルをx=0からアバット | `layout/step1/place_step1_rows.gds` / `.json` |
| step2 | 行内順序最適化(バリセンタ反復、HPWL評価) | `layout/step2/place_step2_ordered.gds` / `.json` |
| step3 | TAP2挿入。セルをTAPセグメントに詰める | `layout/step3/place_step3_tap.gds` / `.json` |
| step4 | FILL分散挿入で行幅を厳密に1620µmへ(**最終**) | `layout/step4/place_step4_fill.gds` / `.json` |

`layout/placement_steps.png` に4STEPの可視化(目視確認用)。

### 13.2 2パス実行

行バッファは最初「シンクをN等分」で入れてあるため、1パス目の配置で得た
実際の行割り当てで入れ直し、2パス目を回す(I2C版 `design_notes.md` §40 と
同じ考え方)。

```sh
scripts/place.py                                    # 1パス目
scripts/insert_row_buffers.py layout/spi_slave_sclk_net_bufth.v \
    layout/spi_slave_sclk_net_pnr.v --nets sclk_buf,shift_clk \
    --cell BUF_X2 --row-assignment layout/row_assignment.json
scripts/run_tests.sh                                # 等価性再確認(36ラン)
scripts/place.py                                    # 2パス目
scripts/verify_placement.py
```

2パス目でチャネル交差が [11,6,6] → [10,5,7]、コア高さが 286.2 → **280.8 µm**
に改善した。

### 13.3 結果

| | |
|---|---|
| コア | **1,620.0 × 280.8 µm = 0.4549 mm²** |
| 行 | 2行 × 64.8 µm、y = 64.8 / 167.4 |
| チャネル | 12 / 7 / 9 トラック = 64.8 / 37.8 / 48.6 µm(ガード2トラック込み) |
| 行0 | 論理セル21個 1,026.0 µm (63.3%) + TAP 43.2 + FILL 550.8 |
| 行1 | 論理セル20個 1,225.8 µm (75.7%) + TAP 43.2 + FILL 351.0 |
| HPWL | 12,255 µm(step1の21,597 µmから43.3%改善) |
| GDS実寸 | 1,632.6 × 280.8 µm(**I2C版コアの幅1,632.6 µmと一致**) |

`scripts/verify_placement.py` の全チェックPASS:
インスタンス被覆(41個、過不足・重複なし)、行内アバット(重なり・隙間ゼロ)、
5.4µmトラックグリッド整合、行幅厳密一致、TAP位置、行のy重なりなし、
GDS参照数(107)と実寸の突き合わせ。

I2C版コア(1,632.6 × 938.9 = 1.533 mm²)の **29.7%**。幅が一致するので
GIO⇔コアの結線・電源メッシュの枠組みをそのまま流用できる。

---

## 14. 配線(Routing)

I2C版(`TR-1um_Async_I2C/script/`)のフローをそのまま使う。あちらは実機
KLayoutでチップ全体DRC/LVSクリーン、SPICEトランジスタレベル検証14/14 PASS
まで到達しているので、**アルゴリズムには一切手を入れない**方針とした。
移植の詳細は [`scripts/PORTING.md`](scripts/PORTING.md)。

- `scripts/i2c_ref/` … I2C版からの**無改変**コピー(23本、参照用)
- `scripts/*.py` … `scripts/port_i2c_scripts.py` が生成する本プロジェクト版
- 変更は全て置換ルールとして `port_i2c_scripts.py` に記述(直接編集しない)

変更したのは (1) ハードコードされた絶対パス → `scripts/spi_config.py` 経由、
(2) トップセル名、(3) `LEF/` → `lef/`、(4) `route_top_pins_nrow_fm.py` の
4行決め打ちをN行に一般化(`n_rows == 4` では原本と完全に同一挙動)の4点のみ。

配置JSONは `scripts/gen_placement_json.py` が `place.py` の step4 出力を
ルータのスキーマ(LEFピン矩形の絶対座標化 + ネット解決)に変換する。
TAP直後のFILL2を `FILLPRI_*` と命名して優先M2コリドーに指定している。

### 14.1 ステージ構成(各STEPのGDSを残す)

| STEP | 内容 | 成果物 |
|---|---|---|
| step5 | 配置GDS + チャネル注釈 | `layout/step5/route_step_1_placement.gds` |
| step6 | チャネル配線。ルータ内部の5パスも各々GDS化 | `layout/step6/route_step_2_{1_tap_power_mesh,2_pass0_per_row_local,3_pass1_high_fo_row_only_adjacent,4_pass2_spanning,5_pass3_force_jog_nets,routed_raw}.gds` |
| step7 | 短絡のリップアップ/再配線 | `layout/step7/route_step_3_ripup_reroute.gds` |
| step8 | トップレベルピンのコア端引き出し | `layout/step8/route_step_4_top_pins.gds` |
| step9 | VDD/GND チップレベルピン | `layout/step9/route_step_5_power_pins.gds` |
| step10 | チャネル圧縮(未使用トラック除去) | `layout/step10/route_step_6_squeezed.gds` |

```sh
scripts/gen_placement_json.py
scripts/route.py                    # step5〜step10 + DRC/接続性チェック
scripts/plot_layout.py layout/step10/route_step_6_squeezed.gds
```

`layout/routing_steps.png` に可視化。

### 14.2 チャネル予算

ルータのジョグ機構は行またぎ1本ごとに新しいトラックを確保するため、
`place.py` のネット交差数ベースの見積もり(12/7/9トラック)では足りず、
最初の試行はチャネル1で溢れた。I2C版と同じく**広めに取って配線し、
step10で圧縮する**方式にした(`spi_config.py` の `ROUTE_CH_HEIGHTS`)。

| | 配線時の予算 | 使用 | 圧縮後 |
|---|---:|---:|---:|
| channel0(下マージン) | 140.0 µm (26 trk) | 8 trk | 42.8 µm |
| channel1(行間) | 900.0 µm (166 trk) | 25 trk | 138.6 µm |
| channel2(上マージン) | 160.0 µm (29 trk) | 3 trk | 22.6 µm(PIN保護分+3.0) |
| **コア高さ** | **1,329.6 µm** | | **324.9 µm(−75.6%)** |

### 14.3 squeeze の PINレイヤ保護

`squeeze_channels_nrow_fm.py` は配線が使っていないYスライスを削除するが、
原本のままだと**トップ辺のPINマーカーも巻き添えで消える**。マーカーはコア
境界のちょうど上に描かれるため「最後に使われたトラックより上のヘッドルームを
潰す」規則に当たり、高さ0になっていた(下辺のマーカーはy=0からコア内側へ
描かれるので無傷)。ラベルもピン形状から外れ、`gen_lef.py` / LVS抽出の
「PIN形状の内側にテキストラベルがある」規約が壊れる。18個が該当した。

`build_y_map` に**保護Y区間**を追加して解決した(`scripts/port_rules.py` の
`SQUEEZE_PIN_PROTECT`)。トップセル自身のPINレイヤ(48/1, 49/1)とテキスト
レイヤ(48/0, 49/0)が占めるY区間を収集・マージし、潰し処理がそこに重なる
部分だけ identity のまま残す。`protect_y` が空なら原本と完全に同一の挙動。

```
protecting 2 Y interval(s) holding PIN markers/labels: 0.0-3.0, 1326.6-1329.6
core height: 1329.6 um -> 324.9 um (-1004.7 um, -75.6%)
```

保護分の3.0 µmだけコアが高くなる(321.9 → 324.9 µm)。検証結果:
PINマーカーは**全32個が 3.0 × 3.0 µm**、潰れたもの0、
どのPIN形状にも入っていない/中心からずれているラベル**0**。

### 14.4 結果

| | |
|---|---|
| **最終GDS** | `layout/step10/route_step_6_squeezed.gds` |
| **コア** | **1,632.6 × 324.9 µm = 0.5304 mm²** |
| DRC | **0違反**(M1/M2の幅・間隔、V1の間隔・M1/M2囲み・GC間隔)。実機KLayoutでもANT(アンテナ)以外の指摘なし |
| 接続性 | **全44ネット160ピン 接続OK・短絡0**(M1優先M2フォールバック検証) |
| 配線内訳 | row-only 34、adjacent-pair 4、spanning 0、high-FO 3、per-row-local 3 |
| ジョグ | 14(強制オーバーラップゾーン)、行またぎ0、spanning 0 |
| リップアップ | 生の配線結果が既に短絡0だったため実質何もせず通過 |
| 優先M2コリドー | 6トラック(x = 13.5 / 18.9 / 548.1 / 553.5 / 1082.7 / 1088.1) |
| TAP電源メッシュ | 4列 × 3チャネル × 2ネット(VDD/GND) |
| トップピン | **25ポート全て引き出し・セルピンまで接続確認済み**(§14.5)。12本が下端、13本が上端。PINマーカーは全て 3.0 × 3.0 µm 正方形、TXM2ラベルは中心 |

I2C版コア(1,632.6 × 938.9 = 1.533 mm²)の **35%**。幅が一致するので
GIO⇔コアの結線枠組みをそのまま流用できる。

**生の配線結果でいきなり短絡0・DRC0違反**だったのは、I2C版が多数の
`PER_ROW_LOCAL_NETS` / `FORCE_JOG_NETS` の個別チューニングを要したのと
対照的。本設計は spanning ネットが0(全76ネット中48が行内で閉じ、
クロック2系統は行バッファで行ローカル化済み)であることが効いている。
指定した `PER_ROW_LOCAL_NETS` は `sclk_buf` / `shift_clk` / `cs_n_buf` の
3本のみ、`FORCE_JOG_NETS` は空。

### 14.5 スカラーポート未引き出しの修正(解決済み)

**症状**: 最終レイアウトで `BUFTH` の入力が配線されていない。調べると
BUFTH に限らず、**スカラーのトップレベルポート9本がまるごと引き出されて
いなかった**(バス16本のみ通っていた)。`sclk` / `cs_n` / `sdio_in` は
`u_bufth_*` の A ピンのネットなので、BUFTH入力の未配線はその一部だった。

**原因**: `scripts/highlight_top_pins_nrow_fm.py`(`route_top_pins_nrow_fm.py`
の `gather_pins` が使う)に**I2C設計のポート名が直書き**されていた。

```python
SCALAR_PORTS = ["rst_n", "scl", "sda_in", "sda_oe", "rx_valid",
                "addr_match", "rw", "busy"]
BUS_PORTS    = {"tx_data": 8, "rx_data": 8}
```

`gather_pins` はこの2つに載っているポートしか探さない。本設計のスカラー
ポート名は1つも含まれず、`BUS_PORTS` だけがたまたま一致していたため
「バスだけ通る」症状になった。移植時に**行数**依存は一般化したが、
**ポート名**依存を見落としていた。

**修正**: `port_rules.py` の `HIGHLIGHT_PORTS_FROM_NETLIST` で、この4つを
**ネットリストのポート宣言から導出**するよう置き換えた(`PORTING.md` §5)。
`assign port = net;` の別名も拾う — スカラーは `netlist_parser` の union-find
リゾルバが解決するが、バスの別名はビットごとの展開が必要なので
`BUS_PORT_NET_ALIAS` に入れる(本設計の `assign rx_data = rx_data_r;` が
まさにこれで、I2C版と同じ形だったのが「バスだけ通った」理由でもある)。
ポート方向 `PORT_DIR` も同じ宣言から導出するようにし、`route.py` の
手書きテーブルを廃止した。

導出結果:

```
SCALAR_PORTS      : byte_end, cs_n, data_oe, dis, rstn, sclk, sdio_in, sdio_oe, sdio_out
BUS_PORTS         : rx_data 8, tx_data 8
BUS_PORT_NET_ALIAS: rx_data -> rx_data_r
```

**再発防止**: 既存の `verify_connectivity_nrow_fm*.py` は**チャネルルータが
記録した44ネットしか見ておらず、スタブ30本(ポート→セルピン1本)が死角**
だった。これがこの不具合が最終GDSまで残った理由なので、
`scripts/verify_port_connectivity.py` を追加した。M1+M2をV1で繋いだ連結成分を
実ジオメトリから作り、各ポートのPINマーカーとそのネットのセルピンが同じ成分に
あることを確認する。`route.py` が step8 直後と step10 の後に自動実行する。

**結果**(再P&R後):

```
step8: 25ポート中12本が下端へ、13本が上端へ引き出し
       ports expected : 25 / pin labels : 27 (VDD/GND込み) / OK
step10: ALL 25 TOP-LEVEL PORTS CONNECTED TO THEIR CELL PINS
        cs_n     -> u_bufth_cs_n.A
        sclk     -> u_bufth_sclk.A
        sdio_in  -> u_bufth_sdio_in.A
```

### 14.6 STDCELL 利用リストと、そこで見つかった2件

`scripts/cell_usage.py` が最終配置から生成する
([`docs/cell_usage.md`](docs/cell_usage.md))。ライブラリ31マクロ中
**使用16種 / 未使用15種**、論理セル41個・842 Tr・145,917 µm²、
物理セル(TAP/FILL)67個・64,035 µm²。

このリストを最終GDSと突き合わせて2件の不具合が出た。

#### (1) LEF の FOREIGN が古く、BUF_X2 の図形が入っていなかった

```
MACRO BUF_X2
    FOREIGN BUF_X1 0.0 0.0 ;
```

`gen_placement_gds_nrow_fm.py` は `macros[type]["foreign"]` で物理セルを
選ぶため、**ネットリストは `BUF_X2` ×4 なのにレイアウトには `BUF_X1` の
図形**が置かれていた。フットプリントが同一なので配置もDRCも配線も何も
気づかず、**LVSで初めて落ちる**類の不具合。`BUF_X2` をGDSに追加した時点で
LEF側が追従していなかった。

`lef/TR-1um_STDCELL.lef` の該当行を修正。`sync_cell_info.py` に
「FOREIGN が MACRO 名と違う」「MACRO に対応するGDSセルが無い」を警告する
チェックを追加した。

> **`BUF_X4` / `BUF_X16` は LEF に MACRO があるのに GDS にセルが無い。**
> 使うと黙って `BUF_X1` になる。実装するかLEFから外すこと。

#### (2) BUF_X2 セル自体のM1間隔違反

FOREIGN を直して `BUF_X2` が本当にレイアウトに入った結果、M1間隔違反が
4件(4インスタンス各1件)出た。セル単体でも再現:

```
BUF_X2: M1 space 1.300 um < 1.400 um  at (x=2.35, y=37.15) cell-local
```

**先に「DRCクリーン」だったのは、(1)のせいで `BUF_X2` の図形が一度も
チップに入っていなかったから**で、このセルは実質DRCを通ったことが
なかった。セル修正後は29セル全てクリーン。

再発防止に `scripts/drc_check_cells.py` を追加した。STDCELLの全セルに
単体でDRC(M1/M2の幅・間隔、V1の間隔・囲み)をかける。**GDSを触ったら回す。**

```sh
scripts/drc_check_cells.py            # 全セル
scripts/drc_check_cells.py BUF_X2     # 1セルだけ
```

修正後の再P&R結果: **DRC 0違反、全ネット接続・短絡0、25ポート全て
セルピンまで接続確認済み、配置とGDSのセル構成が完全一致、`BUF_X2` ×4 が
レイアウトに存在**。コア 1,632.6 × 324.9 µm は変わらず。

---

## 15. 次のステップ

### 完了済み

1. ~~`info.yaml` の `gds.top_cell` 確定~~ → **`tr_1um_3wire_SPI`**
2. ~~RTL設計と検証~~ → **12本 / 187チェック × 3ビュー = 36ラン全PASS**(§4)
3. ~~論理合成~~ → **210等価ゲート / 41セル**(§5)
4. ~~STDCELLへの `BUF_X2` 追加~~ → **実装確認済み**(§12.1)
5. ~~行数の決定~~ → **2行 × 行幅1620 µm**(§12)
6. ~~nrow配置~~ → **全チェックPASS、各STEPのGDSあり**(§13)
7. ~~チャネル配線~~ → **DRC 0違反・短絡0、各STEPのGDSあり**(§14)
8. ~~スカラーポート未引き出しの修正~~ → **全25ポート接続確認済み**(§14.5)
9. ~~STDCELL利用リストとLEF FOREIGN / BUF_X2セルDRCの修正~~ → **29セル全てDRCクリーン**(§14.6)

### 残り

8. **LVS用SPICEネットリスト生成** — I2C版 `gen_lvs_spice_*.py` 相当を移植。
   「LVSが参照する回路 = 機能検証済みのゲートレベルNET」を構造的に保証する
   ため、手書きせずNETから機械的に生成する運用を踏襲する
9. **実機KLayoutでのDRC/LVS** — 現状の独自DRCチェッカーでは0違反、実機でも
   ANT(アンテナ)以外の指摘なし。アンテナは配線長依存なのでGIO統合後に
   再確認し、必要なら該当ネットにアンテナダイオードを入れる
10. **GIO(FRAME)とのトップレベル統合** — コア幅1,632.6 µmがI2C版と一致する
    ので、結線・電源メッシュの枠組みをそのまま流用できる。パッド割り当ては
    §2の通り(信号14本を使い切る)
11. **IRSIM / ngspice によるトランジスタレベル検証** — `hdl/tb_*.v` の187
    チェックをIRSIM側へ移植(I2C版の14項目バッチテストに相当)
12. **MPWエクスポート** — `src/tr_1um_3wire_SPI.gds` / `.cir` を
    `scripts/` のエクスポートスクリプトで機械生成し、由来を
    `PROVENANCE.md` に記録

---

## 付録: 計測方法

- セル寸法: `lef/TR-1um_STDCELL.gds` の各セルbounding boxをgdstkで実測
- トランジスタ数: `TR-1um_Async_I2C/LEF/<cell>.extracted` のMOS素子数を計数
- 上記2つを `lef/cell_info.json` に集約し、`scripts/gen_liberty.py` が合成用
  Libertyを、`scripts/gate_count.py` が規模レポートを生成する
- I2C版の比較値: `TR-1um_I2C_2026/src/tr_1um_i2c_slave_async.gds` の
  `i2c_slave_async_nrow_fm` 直下のreferenceを集計(via/FILL/TAP除外)、
  Tr数は同 `.cir` のsubckt階層を再帰展開して計数
- 論理セル密度27.8%: I2C版の論理セル面積 0.426 mm² ÷ 配置後コアBBOX 1.533 mm²
